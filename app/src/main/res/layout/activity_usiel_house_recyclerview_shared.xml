<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient_pink"
    tools:context=".exercise.home.ivanusielrj.homework1.UsielHouseRecyclerviewActivity">

    <TextView
        android:id="@+id/tvHomeRegister2"
        android:text="Casas Registradas"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="40sp"
        android:textColor="#f4f4f4"
        android:shadowColor="#cb6ce6"
        android:shadowDx="5"
        android:shadowDy="5"
        android:shadowRadius="5"
        android:layout_marginTop="30dp"
        android:fontFamily="@font/pacificoregular"/>

    <ImageButton
        android:id="@+id/ibReturn"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="20dp"
        android:elevation="50dp"
        android:layout_margin="10dp"
        android:src="@drawable/img_backhome"
        android:scaleType="fitCenter"
        android:background="@drawable/img_backhome"
        android:layout_width="40dp"
        android:layout_height="40dp"/>



    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvHouse"
        app:layout_constraintTop_toBottomOf="@id/tvHomeRegister2"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>